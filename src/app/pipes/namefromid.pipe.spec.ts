import { NamefromidPipe } from './namefromid.pipe';

describe('NamefromidPipe', () => {
  it('create an instance', () => {
    const pipe = new NamefromidPipe();
    expect(pipe).toBeTruthy();
  });
});
